<script lang="ts">
	import type { HTMLAttributes } from 'svelte/elements';
	import { cn } from '$lib/utils.js';

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: HTMLAttributes<HTMLTableSectionElement> & { ref?: HTMLTableSectionElement | null } = $props();
</script>

<thead bind:this={ref} class={cn('[&_tr]:border-b [&_tr]:border-border', className)} {...restProps}>
	{@render children?.()}
</thead>
