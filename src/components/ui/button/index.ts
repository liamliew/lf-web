import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize
} from './button.svelte';

export {
	Root,
	type ButtonProps as Props,
	buttonVariants,
	Root as Button,
	type ButtonProps,
	type ButtonVariant,
	type ButtonSize
};
